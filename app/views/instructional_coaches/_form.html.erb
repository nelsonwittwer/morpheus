<%= form_for(@instructional_coach) do |f| %>
  <% if @instructional_coach.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@instructional_coach.errors.count, "error") %> prohibited this instructional_coach from being saved:</h2>

      <ul>
      <% @instructional_coach.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
