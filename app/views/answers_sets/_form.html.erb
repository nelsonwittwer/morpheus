<%= form_for(@answers_set) do |f| %>
  <% if @answers_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answers_set.errors.count, "error") %> prohibited this answers_set from being saved:</h2>

      <ul>
      <% @answers_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :question_id %><br>
    <%= f.number_field :question_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
